// components/admin/templates/infra/InfraMapPanel.tsx
// 'use client';
// import dynamic from 'next/dynamic';

// const MapInner = dynamic(() => import('@components/admin/templates/sidebar/TemplateTruthTracker InfraMap'), { ssr: false });

// export default function InfraMapPanel({ enabled }: { enabled: boolean }) {
//   if (!enabled) return null;
//   return <MapInner />; // the heavy code & tiles only load when opened
// }
