// Replace <ul> in /forks/[template_id].tsx with:
<ul className="text-left pl-4 border-l border-zinc-600">
  {forks.map((f, i) => (
    <li key={i} className="mb-4 relative">
      <div className="ml-2 pl-2 border-l-2 border-blue-600">
        <div className="font-semibold text-blue-400">@{f.handle}</div>
        <div className="text-zinc-300">{f.name}</div>
        <div className="text-xs text-zinc-500">{f.created_at}</div>
      </div>
    </li>
  ))}
</ul>
